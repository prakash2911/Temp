//@ts-nocheck
const url =
  import.meta.env.MODE === "development"
    ? "http://localhost:3000"
    : import.meta.env.VITE_BACKEND_API_BASE_URL;

const token = `Bearer ${
  sessionStorage.getItem("sessionToken")
    ? sessionStorage.getItem("sessionToken")
    : ""
}`;

export default class APIService {
  static async PostFormData(body: any, route: string) {
    try {
      Response = await fetch(url.concat(route), {
        method: "POST",
        headers: {
          // "Content-Type": "application/json",
          "Access-Control-Allow-Origin": "*",
          Authorization: token,
          // "authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJlbWFpbCI6InVzZXJAZ21haWwuY29tIiwidXNlcnR5cGUiOiJhZG1pbiIsImlhdCI6MTY5NjQyNDk0MiwiZXhwIjoxNjk2NDI4NTQyfQ.OmSmhz6TAF6av9yZD3RS0V560AwRI9S-X2wWAN1rLGA",
          // Accept: "application/json",
        },
        body: body,
      }).then(async (res) => {
        return res;
      });
      return await Response.json();
    } catch (error) {
      return console.log(error);
    }
  }

  static async PostData(body: any, route: string) {
    
    try {
      Response = await fetch(url.concat(route), {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Access-Control-Allow-Origin": "*",
          // "Authorization": "Bearer " + sessionStorage.getItem("sessionToken") ? sessionStorage.getItem("sessionToken") : " ",
          authorization: token,
          Accept: "application/json",
        },
        body: JSON.stringify(body),
      }).then(async (res) => {
        return res;
      });
      return await Response.json();
    } catch (error) {
      return console.log(error);
    }
  }
  static async GetData(route: string) {
    try {
      Response = await fetch(url.concat(route), {
        method: "GET",
        headers: {
          "Content-Type": "application/json",
          "Access-Control-Allow-Origin": "*",
          Accept: "application/json",
        },
      }).then(async (res) => {
        return res;
      });
      return await Response.json();
    } catch (error) {
      return console.log(error);
    }
  }
}
